# Agent Studio — утверждённый план, 2026-09-11

Владелец: `/root`. Flashback #405–410 взяты в работу. Внешность #363 вне этой задачи.

## Контракт

Один поток решений модели; независимый сохраняемый исполнитель в Studio.
Операции: команда, наблюдение, выбор, условие, ограниченный цикл, ожидание.
Цель, версия плана, шаг, подтверждённые результаты и причины пауз переживают restart.
Диалог и TTS не задерживают принятые действия. Следующий шаг не ждёт модели.
Наблюдение каждые ~2 секунды, программный heartbeat 30 секунд; модель нужна для
нового решения, существенного события, проблемы или завершения. Сон/отдых —
подзадачи с возвращением. Reconnect сначала сверяет мир/NPC и исход команды;
неизвестный исход нельзя вслепую повторять. Отменённый план не продолжает работу.

Принятое движение сохраняет цель до прибытия либо конкретного серьёзного
препятствия, угрозы, необходимого восстановления или нового явного приказа.
Обычные желания встроенного AI (например, сбор листьев для другой стройки)
не перехватывают управление. Причина и источник каждого прерывания попадают
в диагностику вместе с состоянием lease. Это регрессия уже подтверждённого
игроком исправления Flashback #400 (старый SG #375), а не переоткрытие карточки.

MCP: авторитетные слоты/стаки и вместимость, рецепты, стадии/остатки стройки,
структурированные нужды, собственные отношения, штатные подарки/строительство.
Идемпотентные команды и доступные результаты через существующую командную шину.
Самостоятельное чтение спецификации и продолжений. Навыки workspace: сбор/доставка,
подарки, строительство, восстановление; версии и ссылки на актуальные правила.
Новый навык — кандидат до сценарной проверки, без изменения полномочий/личности.

## Проверки

1. Кокосы: память → вместимость → сбор до заполнения → лагерь → выгрузка.
   Варианты рюкзака, частичного стака и исчезнувших предметов. Проверяется мир.
2. Подарок любимой девушке: собственная симпатия, §153, законное получение,
   доставка и фактическая передача/реакция; сон/движение/отказ получательницы.
3. `bed.basic` с нуля за ≤3 игровых дня, включая добычу, стадии и хотя бы один сон.
   Ведомость из мира (сейчас 4 logs / 5 sticks / 10 rope / 50 leaves, нужен hammer).
   Никакой стройки за агента другими NPC; отдельная проверка Stamina/Energy.
4. Прерывания доставки: угроза, исчезновение цели, reconnect после исполнения
   до подтверждения, разговор и явная отмена. Никаких дублей/ложного успеха.
   Отдельная проверка удержания длительного движения: обычные Build/Gather
   желания и разговор не меняют цель; фиксируются продления/потеря lease,
   источник отмены и фактическое прибытие либо обоснованное прерывание.

Два уровня: детерминированный provider + настоящая симуляция/MCP; настоящая модель
с задачей без готового плана. Отдельные миры/память, production не меняется.
3 набора условий ×2 запуска ×4 сценария на модель; Grok отдельно. Приёмка ≥5/6
на сценарий, все детерминированные проверки проходят, критических ошибок нет.
Функциональный accelerated clock останавливается на inference; отдельная проверка
непрерывного времени честно измеряет задержки. Сравнение с исходной реализацией.

## Диагностика и порядок

Постоянный JSONL и читаемые отчёты: correlation profile/world/NPC/goal/plan/step/
request/command/result; краткое решение, источники, до/после, latency, usage.
Никаких ключей, токенов и скрытых рассуждений. Автосигналы повторов/отказов/
простоя/потери цели — диагностические гипотезы, не подтверждённые баги.
Ограничение объёма/срока, ротация, недоступность логов видима и не останавливает
агента. Отказы/зависание/reconnect должны восстанавливаться по журналу.

Порядок: baseline/диагностика → MCP → исполнитель/единая модель → навыки →
сценарии/регрессии → коммиты и tracker patches → штатная подписанная Studio.
Статус ready_for_test только после коммитов и реальных проверок; fixed — игрок.

## Журнал выполнения

- Продолжение: подготовка активного плана получила общий бюджет 300 секунд
  при прежних четырёх модельных раундах; обычный memory recall — 120 секунд.
  30 focused tests PASS. Grok reasoning bed с новым лимитом дошёл до сна и
  добычи, но повторял PlanInterrupted; диагностический запуск прерван после
  повторения отказов, не является PASS и не заменяет матрицу приёмки.
- Добавлен каталог навыков профиля `MASHA_HOME/skills/catalog.json`: кандидаты
  скрыты от исполняемых инструкций, validated требует версии и ссылки на отчёт;
  замена встроенного навыка требует большей версии. См. AgentStudioSkills.md.
  Изоляция/перечитывание/отказы каталога и диагностические поля: 18/18 PASS;
  Python-гипотезы повторных отказов/запросов без dispatch: 2/2 PASS.

- 2026-09-11: #405–410 назначены /root, in_progress; начат baseline AgentHost.
- Исходный AgentHost suite: 178/179, один сбой в
  `WorldPauseControlsPaidWorkNotPlayerPresenceAndOffDetaches` (поиск
  ARCHIVE_EVENT_FIXTURE дал 0 вместо 1). TRX:
  `Tests/HexLive.AgentHost.Tests/TestResults/agent-plan-baseline.trx`.
  Этот baseline собран до добавления диагностического класса.
- Журнал первого этапа: `StateDirectory/diagnostics/events.jsonl`;
  чтение: `python3 Tools/agent_diagnostics.py <directory> [--turn <turnId>]`.
  Расширение до plan/step и источников следует при интеграции исполнителя.
- Следующий этап: MCP `inventorySummary` (реальная раскладка, свободные слоты,
  возможность дополнить стак) и вклад вместимости одежды; 11/11 серверных tests.
- Команда теперь стартует после commit, до TTS. Заблокированная озвучка не мешает
  приказу; тест проверяет отсутствие повторной отправки после готовности речи.
- Начальные runtime regression checks: 34/34. Исходный сбой archive fixture был
  гонкой ожидания двух любых polls против фактической записи после epoch reset;
  тест теперь ждёт архивируемое событие, сохраняя проверку единственности записи.
- Fleet/Prompt checks: 4/4. Недоступность диагностики показывается в Studio как
  предупреждение, не остановка, и изолирована по профилям.
- Единый model slot реализован: новая реплика отменяет незавершённую autonomy,
  принятый приказ сохраняется, поздний ответ не фиксируется. Даже игнорирующий
  отмену provider не разрешает второй одновременный модельный вызов.
  Проверены 14 scheduling cases: 13 прошли в основном прогоне; один fixture
  проверял порядок cancellation callbacks вместо факта отмены — исправлен,
  целевой повтор 1/1 прошёл. TRX `bug408-single-model*.trx`.
- Промежуточное состояние этого этапа уточнено ниже; production rollout не было.
- Протокол шагов: коммит `d2e36eabf09083488d08bac8bd18d82a45d975a5`,
  12 новых policy cases + 14 scheduling cases: 26/26 PASS, TRX
  `bug409-plan-policy-and-scheduling.trx`. На этом этапе runtime ещё не был подключён; подключение выполнено далее.
- Перечитана актуальная #400: статус fixed подтверждён игроком. Сохраняем её
  регрессии при новой архитектуре; версия о перехвате встроенным AI требует
  новых наблюдений и не считается установленной причиной нынешних #408–409.

### Текущее состояние реализации

- Сохраняемая линейная очередь подключена к runtime и серверным квитанциям.
  Подтверждённый шаг запускает следующий без модели/TTS. Потерянный ответ
  сверяется после восстановления без повторной отправки команды.
- Справочные операции `spec.read`, `skills.list/read`, `recipes.read`, `build.read`
  возвращают ограниченные страницы с источником и версией. Навыки сбора,
  подарка и кровати доступны в workspace. MCP показывает точную вместимость,
  отдельные Energy/Stamina и реальную ведомость видимой стройки.
- Диалог различает новое сообщение и уже обработанную историю (#405).
  Восемь отдельных проверок настоящим Codex и восемь Grok прошли; отчёты
  `Docs/AgentStudioChecks/2026-09-11-behavior-*.json`. Это разговорные проверки,
  НЕ приёмка четырёх игровых сценариев.
- После временной потери attachment сервер восстанавливает непрочитанный inbox
  по приватному ключу (#407). Пока восстановление ограничено временем жизни
  процесса сервера/Studio; сообщения после полного detach не накапливаются.
- Обычная отрицательная оценка уменьшает доверие/симпатию на 5 п.п., серьёзная
  на 10 п.п.; применённые значения и дельты записываются в диагностику (#406).
- Native runtime suite: 4/4 PASS (`bug409-gift-runtime.trx`): последовательность,
  движение, потеря ответа с восстановлением, подарок. Подарок проверяет тот же
  физический экземпляр в инвентаре получательницы, рост её симпатии и единственное
  GiftGiven. Вещь и встреча подготовлены fixture: это проверка исполнителя,
  ещё НЕ самостоятельный поиск/доставка подарка моделью.

Остаются открыты: динамический выбор новых объектов в цикле и расширенные ветвления,
полные четыре игровые цепочки, матрица 3×2 на модель, итоговые общие suites и штатная подписанная упаковка.
Ограниченное восстановление после сна/отдыха и повторение одной команды реализованы ниже.
Карточки #405–410 сохраняют статус in_progress до завершения соответствующей приёмки.

- Последующие проверки: общий AgentHost 211/211 PASS до правки повторного
  планирования; persistence после неё 11/11 PASS. Общий Server 287/287 PASS.
  Коммит 511db1e01 разрешает заменить план после подтверждённого failed, сохранив
  цель; unresolved sending/accepted/unknown по-прежнему блокируют замену.
- Цепочка кокоса через runtime/MCP: настоящий pickup → возвращение → drop
  без промежуточного модельного вызова. Подтверждается прохождение через инвентарь
  и появление собранного запаса в мире. Fixture задаёт один доступный кокос;
  память, самостоятельный выбор и рюкзак этим тестом ещё не покрыты.

- Условия перед командами: свободные слоты и отдельные нужды. Ложное условие
  сохраняет переход вперёд или паузу; отсутствующие данные не считаются нулём.
  Уже отправленная команда всегда сверяется до перехода. Планы с условиями
  используют schemaVersion=2, чтобы предыдущий исполнитель их не исполнил
  без проверок. Поддержка циклов и динамического выбора целей ещё открыта.

- Общая симуляция до rest_until: 1564 PASS, 6 штатно skipped (TRX
  bug409-integrated-simulation.trx). Прогон выявил, что обычный GroundSleep
  бессрочен по §121: ждать его естественного завершения нельзя.
- Добавлен rest_until внутри отслеживаемой команды: отдельный показатель и
  сохраняемый порог, штатный Stop после восстановления, затем следующий шаг.
  Native runtime + справочные проверки 12/12 PASS, включая Energy и Stamina.
  Сон повышает Energy с 0.2 до 0.4 при высокой Stamina; сидячий отдых отдельно
  восстанавливает Stamina при высокой Energy. Во время выполнения нужды не
  подменяются. Безопасность подготовлена fixture, не подавлением живых угроз.
  Полная кровать с добычей и многодневным продолжением ещё не пройдена.

- Диагностика расширена корреляцией plan/step/command и версиями цели/плана,
  машинной причиной квитанции и нуждами до/после. Каждый запрос модели внутри
  справочного цикла тоже получает отдельную latency/usage-запись. Неизвестный
  usage (в том числе текущий Codex adapter) остаётся null; это не полный счёт
  провайдера за неуспешные запросы. CLI Tools/agent_diagnostics.py --plan <hash>
  выводит цепочку и отдельно считает известные/неизвестные токены.
  Проверки диагностики/runtime/knowledge 25/25, transport usage 4/4 PASS;
  фильтрация отчёта и неизвестный расход проверены отдельным CLI fixture.

- Подтверждённые команды сохраняются в памяти цели (64 записи), переживают
  смену сегмента/restart и входят в следующий контекст вместе с оставшимися
  шагами. Это исправляет потерю прогресса после выгрузки. Условия не создают
  ложных completed. Persistence/native runtime: 21/21 PASS.
- Первый настоящий Codex coconut case (fixture 1, repetition 0) прошёл: три
  предмета собраны, доставлены и выгружены, модель завершила цель. 7 модельных
  запросов с учётом справки, 8 отправок команд, 138 игровых тиков. Был безопасный
  отказ устаревшего индекса и перепланирование; это ещё не полная матрица.

- Codex transport получил штатный --output-schema. Открытые maps аргументов
  передаются закрытой схемой пар name/value и преобразуются обратно до строгой
  игровой проверки; дубли имён отвергаются. Это устраняет свободный формат
  ответа как причину лишней JSON-скобки. Format/native runtime 21/21 PASS;
  модель приняла схему, полная игровая приёмка ещё продолжается. Документация:
  https://learn.chatgpt.com/docs/non-interactive-mode .

- Видимые люди теперь включают собственное отношение наблюдателя к ним;
  это позволяет выбирать адресата подарка по симпатии, не раскрывая обратные
  скрытые чувства. Чтение не создаёт знакомство. Общий Server: 291/291 PASS
  (`bug409-server-planning-final.trx`), включая прежний контракт rest_until.
- MCP/навык выгрузки объясняют сдвиг физических индексов: отдельные предметы
  можно выгружать очередью по убывающим индексам. Навык кровати различает
  действующую стройку и требования к размещению новой площадки.

- Реальная модель подарка выявила повтор talk_to после Unreachable: причина
  терялась между квитанцией и промптом. Теперь Command.Reason сохраняется и
  передаётся модели; произвольный серверный текст вместо кода отбрасывается.
- Recall обновляет последние три выбранные справки в пределах активной версии
  цели до первого модельного вызова. Это свежие чтения с новыми source hashes,
  без fallback на устаревший текст и без повторных модельных раундов. Смена
  цели/мира/NPC очищает список. Persistence/refs/runtime: 28/28 PASS.

- Первый самостоятельный Codex gift case (1,0) PASS: модель прочла навык и
  §153, выбрала девушку по собственной симпатии, подобрала вещь и передала её.
  Проверены инвентарь получательницы и единственный GiftGiven. 5 модельных
  вызовов (с учётом справки), 2 команды, 177 тиков, 65 секунд стенда.
  Протокол с промежуточными решениями/наблюдениями сохранён в
  Docs/AgentStudioChecks/2026-09-11-codex-gift-probe-pass.json .
  В harness добавлены явные model-backed cases кокосов, подарка и кровати;
  кровать и вся матрица ещё не подтверждены. Это isolated WorldHost/MCP/native
  executor с ускоряемыми тиками, не непрерывный production RunLoop.

- Потерянный ответ теперь сверяется автоматически (до трёх повторных чтений),
  сохраняя lease и прежнюю идентичность команды. Подтверждённое completed
  продолжает очередь без модели только для CommandOutcomeUnknown и активной
  цели; явная/критическая пауза остаётся. 25/25 focused checks PASS, включая
  повторное подключение и продолжение без повторной отправки.

- Прогон кровати показал повтор Harvest без нужного инструмента. MCP теперь
  сообщает требования ANY-OF capabilities, факт их выполнения текущим телом/
  инвентарём, базовую длительность и выходы взаимодействия, возможности видимого
  инструмента. Это данные действующих каталогов, не подсказка с готовым планом.
  Item observations + MCP contract: 21/21 PASS.

- Полный AgentHost после сверки потерянных ответов: 240/240 PASS
  (`bug409-host-integrated-current.trx`). Дополнительная проверка ограничивает
  неопределённый исход тремя чтениями, одной отправкой и нулём вызовов модели.
- Стенд кровати готовит дом штатным blueprint/build bootstrap и оставляет
  кровати пустыми стройками. Успех сверяется по исходным якорям: штатная сборка
  создаёт новый objectId. Схема реального fixture —
  `Docs/AgentStudioChecks/2026-09-11-bed-fixture.svg` (r=1.5 wu, шаг 0.375 wu,
  37 внутренних узлов). Других жителей удаляют до запуска одиночного сценария,
  чтобы их заморозка не создавала искусственное массовое обезвоживание.
  Собственные нужды Маши далее изменяет симуляция. Аварийное восстановление
  разрешено при паузе цели, её исходный текст обязан сохраняться.
- Следующий диагностический Codex bed case (1,0) остановлен после повторения
  Craft вместо чтения рецептов; полная кровать не подтверждена. Найдена потеря
  справок в отдельном follow-up query_known_objects. Поиск перенесён внутрь
  общего recall: источники и результат поиска сохраняются, последующие
  memoryRequests обрабатываются до commit. Общий лимит чтения не увеличен.
- Исполнитель сохраняет управление между завершением сегмента активной цели и
  следующим решением модели; scheduler продлевает lease без модели. Передача
  следующей очереди не возвращает тело встроенному ИИ. Проверки executor,
  поиска и scheduler: 39/39 PASS (`bug409-context-lease.trx`).
- Предварительная проверка штатной подписи macOS прошла: сертификат и доступ
  к закрытому ключу доступны. Сборка и публикация Studio ещё не выполнены.

- Освобождение межсегментного lease при паузе мира, паузе цели и завершении
  цели: 3/3 PASS (`bug409-plan-lease-release.trx`).
- Первый Grok bed probe (1,0): 3 вызова, 4 отслеживаемые команды, сон состоялся,
  3413 тиков; остановлен ошибкой формата продолжения (action вместо очереди),
  кровать не построена. Контроллер теперь переводит одиночное продолжение
  активной цели в один сохраняемый шаг до commit. Неопределённая/активная
  очередь защищена от неявной замены, аварийная пауза сохраняет action.
  Policy/runtime/ownership: 38/38 PASS. Событие decision.normalized явно
  отмечает преобразование; сырые модельные ответы в приёмке остаются сырыми.
- Игровой отчёт отдельно фиксирует реальные справочные чтения и аварийные
  действия. Приёмка кровати требует build.read + recipes.read, подарка —
  spec.read; физический успех без консультации правил не проходит этот критерий.

- Противоречивые action + executionPlanUpdate исправляются тем же модельным
  потоком до любых побочных эффектов, в существующем бюджете раундов. Проверки
  bounded repair + references/query: 21/21 PASS. Grok получил штатный json_schema
  (https://docs.x.ai/developers/model-capabilities/text/structured-outputs).
  Transport: 4/4; формат/планы: 22/22 PASS. Реальный запрос схему принял,
  но последующая семантическая ошибка перехода корректно остановила исполнение;
  успех игрового сценария этим не подтверждён.
- Добавлен полный Runtime.RunAsync + WorldHost.Run с непрерывными тиками и
  реальным MCP command path. Разговор во время длительного сна сохраняет ID,
  статус и курсор очереди; явная отмена или событие Threat не допускают
  следующую команду. Оба case PASS (`bug409-live-clock-interruption.trx`, 17 s).
  Событие угрозы в этом контрактном тесте синтетическое, это не прогон с живым
  волком. Потеря ответа/reconnect отдельно проверяются native runtime tests.

- Полный AgentHost с обработкой конфликтов, продолжениями и живым циклом:
  254/254 PASS (`bug409-host-live-context-integrated.trx`, 2m55s).
  Дополнительно исправляются два конкретных отказа валидации плана в прежнем
  лимите раундов; сетевые/авторизационные ошибки не перегенерируют решение.
- build-bed v4 описывает раскрытие зависимостей, чтение полного рецепта после
  индекса, проверку capabilities инструмента и craft_item по recipeGoal.
  Стоимость ресурсов по-прежнему читается из текущего каталога.

- Первая полная матрица кокосов Grok: **0/6**, 41 модельный вызов и 46 команд.
  Частые причины: повтор уже завершённого GoHome, неверные переходы и один
  FalseCompletion после отказа выгрузки. Результат не принят; сводка сохранена
  в `Docs/AgentStudioChecks/2026-09-11-grok-coconut-baseline-summary.json`.
- `Tools/agent_gameplay_summary.py <matrix-directory>` считает все шесть случаев
  каждого сценария, не подменяет отсутствующие успехами и отвергает дубликаты
  повторов. Порог ≥5/6 требует полного набора; любой FalseCompletion запрещает
  приёмку. Три Python-проверки PASS. Прогоны с угрозой/reconnect учитываются
  отдельно и явно не объявляются модельной матрицей этим инструментом.
- MCP inOwnCamp даёт фактическое присутствие тела в своём лагере; навык доставки
  v4 отделяет его от выгрузки. Четыре server observation checks PASS.
- Исправление невалидного плана теперь видит конкретный отвергнутый JSON;
  постоянный JSONL содержит только код ошибки. Complete при active/paused
  очереди запрещён и в recall, и перед транзакцией памяти. Отмена остаётся
  доступна. Проверки references/persistence/schema: 33/33 PASS.

- Отдельная reasoning-модель `grok-4.20-0309-reasoning` прошла coconut (1,0):
  4 модельных вызова, 7 команд, 138 тиков. Прочитаны skills.list/read, собраны
  три экземпляра, доставлены и выгружены. Полный протокол:
  `Docs/AgentStudioChecks/2026-09-11-grok-reasoning-coconut-probe-pass.json`.
  Это другая модель; результат не заменяет baseline non-reasoning 0/6.

- Добавлен repeat=1..64 для действия с неизменными аргументами, суммарно до
  256 отправок на план. Итерация сохраняется отдельно от курсора; каждая имеет
  уникальную квитанцию и повторную проверку условия. schemaVersion=3 защищает
  от исполнения старой Studio. Это ограниченный цикл, не динамический подбор
  новых объектов. Native крафт реально изготовил две верёвки и потратил две
  ведомости волокна, без модели между итерациями. Четыре целевые проверки PASS
  (`bug409-native-craft-repeat.trx`); 66 остальных checks прошли в первом прогоне,
  ошибочное ожидание автоматического resume в новом restart fixture исправлено.
