# Разделы спеки, на которые код ССЫЛАЕТСЯ, но которых в Spec/ НЕТ.
#
# Это долг, а не разрешение. Каждая строка означает: итерацию сделали, код
# помечен номером, а текста раздела не написали — и ссылка §N в комментарии
# обещает объяснение, которого не существует. В одноблочном spec.md этот долг
# был невидим: чтобы его увидеть, надо было сверить 3773 ссылки с 22 тысячами
# строк. После разреза он считается за секунду, и вот он весь.
#
# Гейт SpecStructureGate падает на НОВУЮ висячую ссылку и только ругается на
# закрытую — чтобы написать недостающий раздел можно было, не правя этот файл
# в том же коммите.
#
# ⭐ Эти номера НЕ свободны. Tools/spec_new.py выдаёт max+1 и никогда не
# переиспользует дыры: занять §87 новой темой значит сделать шесть существующих
# ссылок не мёртвыми, а ЛЖИВЫМИ — они начнут указывать на чужой текст.

29G   спальные места рядом (0/+1/-1); реализовано, описано только в комментариях ExecutionSystem
29H   лимит бутылок (MaxCarriedInstances=1); BuildHutTest/BuildHutTestWorld.cs
87    абьюз/ненависть; 6 файлов, в т.ч. Runtime/Balance/Spec81.cs
88    абьюз; SimBalance.cs и рядом
90    абьюз/агрессия; 7 файлов, в т.ч. Runtime/Balance/Spec81.cs
92    арена абьюза; AbuseTest/AbuseTestBootstrap.cs
95    арена абьюза; AbuseTest/AbuseTestBootstrap.cs
96    вид актёра; Wearing/NpcActorView.cs
98    дистанция взаимодействия; 4 файла, в т.ч. Helpers/InteractionReach.cs
103   ⭐ самый крупный долг: 20 файлов, ядро ИИ (AI/NpcMind.cs и далее)
