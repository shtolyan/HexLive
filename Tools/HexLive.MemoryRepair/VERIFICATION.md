# Bug #373: сохранение документов Маши

Проверено 2026-09-09. Базовый commit: f43fc2316.

- AgentCore: 40 passed, в том числе 9 новых проверок MemoryDocumentSyncTests.
- AgentHost: 52 passed; полный набор повторён после последних изменений.
- SpecStructureGate: 6 passed. `git diff --check`: без ошибок.
- Studio Release и инструмент MemoryRepair собраны. Предупреждения: недоступный
  NuGet vulnerability feed, CA1416, несовпадение версий Avalonia/Roslyn analyzer;
  это не аудит уязвимостей и не проверка Windows.
- Полный backup workspace: `~/Library/Application Support/HexLive/MemoryBackups/Masha-before-bug-373-20260909`.
- Штатное восстановление выполнено дважды для `~/Library/Application Support/Masha`.
  USER.md/MEMORY.md содержат одну копию. `.state/state.json` побайтно совпадает с
  backup: 4 core-факта, 0 привязанных собеседников, 5 миров. Имя и отношения не изменены.
- Новый пакет: `Build/AgentStudio/preview-20260908-212519/Agent Studio.app`.
  SHA-256 общего ядра: `e6fcaddd2e8bf34a5c152c2a102ebaa577caa319b02716015184aa94845e6e47`.
- Установлен `/Applications/Agent Studio.app`; подпись проверена, хеш ядра совпал.
  После запуска видны Маша, прежняя модель и `Stopped · no requests from Agent Studio`.
  В открытом редакторе USER.md визуально подтверждена одна копия текста и прежние 100%.
  Во время установки прежний пакет уже отсутствовал по стандартному пути; новая
  копия установлена на свободное место, резервирование прежнего приложения не заявляется.

Проверены: отсутствие дублей в новых/старых файлах; сохранность отдельной ручной
заметки; удаление и замена seed-фактов; перезапуск; блокировка устаревшего upsert;
две редакции до хода; восстановление исходной редакции; повтор после сохранения
состояния; изменение процентов только основному собеседнику и последующая динамика;
ошибочные проценты/маркеры; удаление строки процентов без обнуления отношений;
исключение удалённого факта из recall без удаления импортированного оригинала.

Модель, TTS и живой мир для этих проверок не запускались. Сервер и игровой клиент
не обновлялись. Применение смысловых правок относится к Save в Studio для USER.md,
MEMORY.md и управляемых self-заметок SOUL.md; архивы/снимки мира не становятся
универсальным редактором игрового состояния. История и полная резервная копия
позволяют вернуть удалённые текстовые дубли, если потребуется.
