# wardrobe — конвейер добавления одежды

Превращает архив с ассетами DAZ в готовые Wear-префабы HexLive. Стадии
разнесены по отдельным командам намеренно: агент-надзиратель запускает их по
одной и читает JSON-отчёт, поэтому падение — это маленький названный шаг, а не
непрозрачный часовой скрипт.

```bash
python -m wardrobe check
python -m wardrobe dress --drop sweetjane --garment "<...>/Sweet Jane Skirt.duf" --garment "<...>/Sweet Jane Tank.duf"
python -m wardrobe build --drop sweetjane
python -m wardrobe show  --drop sweetjane
```

## Что делает каждая стадия

| стадия | что происходит |
|---|---|
| `check` | пути, внешние программы, живость DAZ Studio |
| `dress` | по каждой девушке: открыть сцену, снять всё надетое, надеть новое, выгрузить FBX |
| `build` | перенести текстуры в проект и составить черновик манифеста |
| `show` | показать манифест вместе с доказательствами (облако вершин по зонам тела) |

Дальше Unity сама собирает префабы: экстрактор читает
`Assets/Editor/WearDrops/<drop>.json`, поэтому **новая одежда не требует правок C#**.

## Что решает человек

Черновик манифеста — это предложение, а не приговор. Проверять нужно:

- `simId`, `name`, `folder` — генератор даёт заглушки вроде `clothing.skirt`;
- `layer` — всегда предлагается `Wear`;
- `slots` — выводятся из геометрии, рядом лежит `_measured.zones` с долями
  вершин по зонам тела, чтобы догадку можно было проверить, а не поверить в неё.

Повторный `build` **не трогает** уже проверенные поля: он обновляет только
замеры и добавляет вещи, которых раньше не было.

## Сессия-надзиратель

Голый конвейер не умеет усомниться в результате: стадия вернула «успех» с
пустым списком — он поедет дальше. Поэтому каждый прогон бота поднимает
**отдельную сессию Claude Code** (`wardrobe/supervisor.py`), которая выполняет
стадии, читает их отчёты и вмешивается только когда что-то сломалось.

Разрешения выданы **списком, а не «можно всё»**: агент без человека за спиной с
`bypassPermissions` может выполнить в репозитории что угодно. Список —
`supervisor.DEFAULT_ALLOWED_TOOLS`.

Разовая настройка: CLI хранит свою авторизацию отдельно от десктопного
приложения, поэтому один раз надо войти вручную. В PowerShell:

```bash
& "$env:LOCALAPPDATA\Packages\Claude_pzs8sxrjxfjjc\LocalCache\Roaming\Claude\claude-code\2.1.219\claude.exe"
```

и внутри `/login`. Ключ API не нужен — прогоны идут по подписке.

Путь именно такой, длинный, **не** `%APPDATA%\Claude\...`: см. про виртуализацию
ниже — короткий вариант работает только у процессов, порождённых самим
приложением, и молча не находится из обычного терминала.

## Грабли, за которые заплачено

- **Сервер DAZ принимает поле `args`, но никогда его не связывает** — скрипт
  падает с `ReferenceError: Can't find variable: args`. Клиент вшивает
  параметры в текст скрипта.
- **Экспорт FBX только через `DzFbxExporter` напрямую**, с `RunSilent`: иначе
  модальный диалог вешает сервер. MCP-инструмент `daz_export_fbx` сломан —
  зовёт несуществующий `DzExportMgr.doExport`.
- **`IncludeMorphs=false`** превращает 64 МБ в 9 МБ и ничего не теряет.
- **Габаритный ящик — плохое описание вещи.** У расклешённой юбки он достаёт до
  плеч, у пары рукавов включает пустую грудь между ними. Слоты считаются по
  облаку вершин.
- Плагин DAZ подхватывается только при СТАРТЕ студии, и сервер надо включить
  руками: Window → Panes → Daz Script Server → Start Server.
- **`%APPDATA%` у Store-приложений виртуализован, и это бьёт в обе стороны.**
  Десктопный Claude — Store-пакет, поэтому `Roaming\Claude\claude-code`
  существует **только** для процессов, которые он сам породил. Обычный
  PowerShell пользователя того же пути не видит («is not recognized»), а
  Store-Python видел в `Roaming\Claude` один файл там, где PowerShell
  показывает два десятка папок — причём `Path.exists()` молча отвечал False,
  выдавая отказ в доступе за «файла нет». Лечится двумя вещами: венв на
  управляемом CPython от uv (`uv python install 3.12` → `uv venv --python 3.12`)
  и обращение к CLI по физическому пути
  `%LOCALAPPDATA%\Packages\Claude_*\LocalCache\Roaming\Claude\claude-code\`,
  который резолвится откуда угодно.
- **У DAZ два способа носить вещь.** Одежда конформится (DzFigure со
  следованием), аксессуар парентится к кости. Считать только первое — значит
  объявить надетый убор ненадетым.
- **Один продукт может возить и `wearable`-пресеты, и `scene_subset`.** Первые
  надевают вещь на выбранную фигуру, вторые кладут геометрию в корень сцены.
  Внутри одного продукта пресет всегда главнее.

## Секреты

Токен Telegram-бота и прочие ключи — в `.env` рядом с этим файлом, он в
`.gitignore`. В коде токенов нет.
